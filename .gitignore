.DS_Store
Thumbs.db
.claude/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Packaged theme build artifact — large binary that duplicates the
# wordpress-theme/ultimatum-finesse/ source tree; kept out of version control.
wordpress-theme/ultimatum-finesse.zip
